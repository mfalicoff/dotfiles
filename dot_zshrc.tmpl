# Managed by chezmoi

# ─────────────────────────────────────────────
# OH MY ZSH
# ─────────────────────────────────────────────
export ZSH="$HOME/.oh-my-zsh"

plugins=(
  git
  azure
  bun
  docker
  fzf
  zsh-autosuggestions
  zsh-syntax-highlighting
)

source $ZSH/oh-my-zsh.sh
export PATH="$HOME/.local/bin:$PATH"

# ─────────────────────────────────────────────
# PROMPT — Pure
# ─────────────────────────────────────────────
autoload -U async && async
autoload -U prompt_pure_setup
prompt_pure_setup

# ─────────────────────────────────────────────
# ENVIRONMENT & COMPLETION FIXES
# ─────────────────────────────────────────────
export EDITOR="nvim"
export XDG_PICTURES_DIR="$HOME/screenshots"

# Prevent automatic completion menu popup while typing
setopt NO_AUTO_LIST
setopt NO_MENU_COMPLETE
ZLE_REMOVE_SUFFIX_CHARS=$' \t\n;&|'
export ZSH_AUTOSUGGEST_STRATEGY=(history)

# ─────────────────────────────────────────────
# HISTORY
# ─────────────────────────────────────────────
HISTSIZE=10000
SAVEHIST=10000
mkdir -p "$HOME/.local/share/zsh"
HISTFILE="$HOME/.local/share/zsh/history"

# ─────────────────────────────────────────────
# ALIASES
# ─────────────────────────────────────────────
alias k="kubectl"
alias ll="eza -l --icons=auto --git"
alias ls="eza --icons=auto"
alias ndev="nix develop --command zsh"

# ─────────────────────────────────────────────
# TOOLS
# ─────────────────────────────────────────────

# direnv
eval "$(direnv hook zsh)"

# fzf
[ -f ~/.fzf.zsh ] && source ~/.fzf.zsh
export FZF_CTRL_R_OPTS="--bind 'enter:accept'"

# ─────────────────────────────────────────────
# TMUX — Auto-attach or create session
# ─────────────────────────────────────────────
if [[ -z "$TMUX" ]]; then
  tmux attach 2>/dev/null || tmux
fi

{{ if eq .chezmoi.os "darwin" }}
# ─────────────────────────────────────────────
# PATH — Homebrew (Apple Silicon)
# ─────────────────────────────────────────────
eval "$(/opt/homebrew/bin/brew shellenv)"
fpath+=("$(brew --prefix)/share/zsh/site-functions")
{{ end }}